<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.codev.mapper.PortfolioMapper">
	<select id="selectById" resultType="Portfolio" parameterType="String">
		select * from portfolios where id = #{ id }
	</select>
	<select id="selectAll" resultType="Portfolio">
		select * from portfolios
	</select>
	<insert id="addPortfolio" parameterType="Portfolio">
		insert into portfolios (title, body, author, written_date, image_url) values (#{title}, #{body}, #{author}, sysdate(), #{image_url})
	</insert>
</mapper>